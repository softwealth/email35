{
  "name": "email35",
  "version": "1.0.0",
  "description": "Email35 — Pay-to-email service backend",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/functions": "^3.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^19.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
